classdef FixedAttenuatorConfig < BtcBaseClass
    % FixedAttenuatorConfig Build-time configuration class for the FixedAttenuator block
    % Usage:
    %   aConfig = FixedAttenuatorConfig
    %   Copyright 2023 Bose Corporation

    properties (Description='TuneDefault')
        A single = -10
    end

    methods
        %----------------------------------------------------------------------
        function this = FixedAttenuatorConfig()
            % FixedAttenuatorConfig constructor: assign component property objects
            % and adjust component property defaults and/or lock values
        end
        %----------------------------------------------------------------------
        function Validate(this)
            % Validate called from mask initialization to check settings ok
            Validate@BtcBaseClass(this);
        end
    end
end
