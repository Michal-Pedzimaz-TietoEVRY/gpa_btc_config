classdef MedusaCaeEnableConfig < BtcBaseClass
    % MedusaCaeEnableConfig Build-time configuration class for the MedusaCaeEnable block
    % Usage:
    %    aConfig = MedusaCaeEnableConfig
    %

    %   Copyright 2023 Bose Corporation

    properties(SetAccess=private)
        InitialValue        int32       = 1
    end

    methods
        %----------------------------------------------------------------------
        function this = MedusaCaeEnableConfig()
            % MedusaCaeEnableConfig constructor: assign component property objects
            % and adjust component property defaults and/or lock values

        end
        %----------------------------------------------------------------------
        function Validate(this)
            % Validate called from mask initialization to check settings ok
            % Call base class and check for any cross-property interactions
            Validate@BtcBaseClass(this);
        end

    end
end
